{
  "name": "fullstack-app",
  "version": "1.0.0",
  "scripts": {
    "start": "node server.js",
    "build": "cd frontend && npm install && npm run build"
  },
  "dependencies": {
    "bootstrap-icons": "^1.11.3",
    "express": "^4.18.2"
  }
}
